# === Configuración de la base de datos (MongoDB Atlas) ===
MONGODB_URI=mongodb+srv://TU_USUARIO:TU_PASSWORD@TU_CLUSTER.mongodb.net/TU_DATABASE?retryWrites=true&w=majority

# === Clave secreta para JWT (autenticación) ===
# IMPORTANTE: Genera una clave aleatoria segura (mínimo 32 caracteres)
JWT_SECRET=tu_clave_super_secura_aqui

# === Puerto del servidor ===
PORT=10000
